setup:
  addons:
    - plan: mongolab:sandbox
      as: DATABASE
  config:
    NODE_ENV: production
    USE_MOCK_DATA: false
    PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: true

build:
  languages:
    - nodejs

run:
  web: npm start
  worker: npm run scheduler